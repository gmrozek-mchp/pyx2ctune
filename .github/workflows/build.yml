name: Build Executables

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: pyx2ctune-linux
          - os: macos-latest
            artifact: pyx2ctune-macos
          - os: windows-latest
            artifact: pyx2ctune-windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install Linux display dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb-xinerama0 \
            libxcb-cursor0 \
            libxkbcommon-x11-0 \
            libegl1

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pyinstaller

      - name: Build application
        run: pyinstaller pyx2ctune.spec

      - name: Package (macOS DMG)
        if: runner.os == 'macOS'
        run: |
          mkdir -p dist/dmg
          cp -R dist/pyx2ctune.app dist/dmg/
          ln -s /Applications dist/dmg/Applications
          hdiutil create -volname pyx2ctune -srcfolder dist/dmg \
            -ov -format UDZO dist/${{ matrix.artifact }}.dmg

      - name: Package (Linux tar.gz)
        if: runner.os == 'Linux'
        run: |
          cd dist
          tar czf ${{ matrix.artifact }}.tar.gz pyx2ctune/

      - name: Package (Linux .deb)
        if: runner.os == 'Linux'
        run: |
          PKG=dist/deb-staging
          VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          mkdir -p $PKG/opt/pyx2ctune
          cp -R dist/pyx2ctune/* $PKG/opt/pyx2ctune/
          mkdir -p $PKG/usr/local/bin
          ln -s /opt/pyx2ctune/pyx2ctune $PKG/usr/local/bin/pyx2ctune
          mkdir -p $PKG/usr/share/applications
          cat > $PKG/usr/share/applications/pyx2ctune.desktop <<'DESKTOP'
          [Desktop Entry]
          Name=pyX2Ctune
          Comment=MCAF motor control tuning tool
          Exec=/opt/pyx2ctune/pyx2ctune
          Terminal=false
          Type=Application
          Categories=Development;Engineering;
          DESKTOP
          sed -i 's/^          //' $PKG/usr/share/applications/pyx2ctune.desktop
          mkdir -p $PKG/DEBIAN
          cat > $PKG/DEBIAN/control <<EOF
          Package: pyx2ctune
          Version: ${VERSION}
          Section: electronics
          Priority: optional
          Architecture: amd64
          Depends: libxcb-xinerama0, libxcb-cursor0, libxkbcommon-x11-0, libegl1
          Maintainer: Microchip Technology <support@microchip.com>
          Description: MCAF motor control tuning tool
           GUI application for PI gain tuning of Microchip MCAF
           motor control firmware using pyX2Cscope.
          EOF
          sed -i 's/^          //' $PKG/DEBIAN/control
          dpkg-deb --build $PKG dist/${{ matrix.artifact }}.deb

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cd dist
          Compress-Archive -Path pyx2ctune -DestinationPath ${{ matrix.artifact }}.zip

      - name: Upload artifact
        if: runner.os != 'Linux'
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: |
            dist/${{ matrix.artifact }}.zip
            dist/${{ matrix.artifact }}.dmg

      - name: Upload artifact (Linux tar.gz)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}-tar
          path: dist/${{ matrix.artifact }}.tar.gz

      - name: Upload artifact (Linux .deb)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}-deb
          path: dist/${{ matrix.artifact }}.deb

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/**/*.zip
            artifacts/**/*.tar.gz
            artifacts/**/*.dmg
            artifacts/**/*.deb
